## First create a .env file and add the **Gemini** api key to the file like this 
  API_KEY=YOUR_API_KEY

## To install the dependencies use

  pip install -r requirements.txt

## To run the api use 

  flask run 
  or 
  python app.py

